import { ScullyConfig } from '@scullyio/scully';
export const config: ScullyConfig = {
  projectRoot: "./src",
  projectName: "play-jamstack-ng-explorer",
  outDir: './dist/static',
  routes: {
  }
};